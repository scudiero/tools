
//======================================================================================================================
// Deferred approval (esigs) definitions
//======================================================================================================================
esiglist:<name>|<stepName>|function|<functionName>|;//<emits>

esiglist:SC|Subcomittee Chairs|function|SubcomitteesEsigs|; //emits steps: Gen Ed Subcommittee Chair (isGenEd), STEM Subcommittee Chair (isSTEM), Liberal Arts Subcommittee Chair (isLA)


*** If defering the approval, make sure the FIRST esig step is an FYI step

*** That is in the showrec.plt, so you can just change the label of the button right in showrec.plt
%?isesig?%	<input type="button" id="esig_btn" class="btn btn-green b_medium esig" value="Approve" />

The eSig function should be doing the replacement, no replacement happens on the return value from the function, similar to how normal wffuncs do the replacement in the function and the return value isn’t replaced.
